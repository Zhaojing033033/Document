安全验证流程

签名流程
1.对按首字母将参数排序，并且用&符号链接，组成带签名字符串
2.对带签名串使用sha-2算法进行摘要并转换成16禁止
3.使用银联颁发给商户的的RSA私钥进行签名操作
4.最后对签名采用base64编码（可选）
5.将签名放入表单域中，当作参数传入后台

验证流程
1.获取到参数之后，按首字母排序，并且&符号链接组成带签名字符串
2.对带签名字符串使用sha-2算法进行摘要，并转成16进制
3.在使用商户入网时的公钥进行 签名信息做签名操作，
4.然后对比参数